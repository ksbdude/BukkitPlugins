name: Arena
main: me.ksbdude.Arena
version: 1.0
description: >
             Custom Plugin by ksbdude.
commands:
  join:
    description: joins arena
     usage: /<command> [id]
  create:
    description: creates arena
    usage: /<command>
  remove:
    description: removes arena
    usage: /<command> [id]
  leave:
    description: leaves arena
    usage: /<command>
  sendme:
    description: Sends a player a message!
  setslapperspawn:
    description: Sets slapper spawn!
